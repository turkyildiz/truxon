---
name: nas-access
description: aida-nas over Tailscale — SSH WORKS (ssh turkyildiz@100.89.140.98); Docker jobs live there (backup, fuel, Valhalla)
metadata: 
  node_type: memory
  type: project
  originSessionId: a28d9126-d517-4423-90d2-26d2f9088c49
---

**`aida-nas` is a FULL LINUX SERVER** (not a constrained UGOS appliance) — runs Docker and can host VMs. So deploy the parked jobs natively: Docker containers + cron/systemd timers, egress-only over Tailscale, never open a port. (Confirmed by owner 2026-07-19.)

The NAS runs the `truxon-backup` compose project (nightly encrypted backups) for [[project-truxon]]. Goal: SSH in from this dev box to sync the hardened `deploy/backup/` scripts and set `WATCHDOG_REPORT_KEY` in the compose env so the nightly backup pings the watchdog heartbeat (else the `backup_fresh` check alarms after 26h — expected, not a real failure, until this is done).

**NAS = `aida-nas`** on the user's Tailscale tailnet (owner turkyildiz@gmail.com): Tailscale IP `100.89.140.98`, MagicDNS `aida-nas.tail2c5ca.ts.net`, LAN IP `192.168.30.18` (DIFFERENT physical network from the dev box — the matching 192.168.30.x subnet is coincidence; LAN route is NOT usable). Route must be Tailscale.

**SSH ACCESS ESTABLISHED (2026-07-19):** `ssh turkyildiz@100.89.140.98` (or `@aida-nas`) with the dev-box ed25519 key, over Tailscale. Dev box `ikedev` (100.106.197.110) joined the tailnet. NAS SSH is on port 22 but was firewalled to LAN — user added a UGOS firewall allow for the Tailscale range (100.64.0.0/10). Key lives in `/home/turkyildiz/.ssh/authorized_keys`.

**NAS environment (`hostname Aida`, uid 1000 turkyildiz, groups admin/users/ughomeusers):** full Linux 6.12 x86_64, **Docker 29.4.3 + compose v5.1.3**, `pg_dump 15`, `gpg 2.2.40`, `python3 3.11`, node v18 (but **no npm/npx**), user `cron` active. Storage: **/volume1 = 179T (100T free)**, /home on same volume. **Timezone Canada/Saskatchewan (UTC-6) = same as Central America** (no DST) — so fuel-fetch cron 03:00/16:00 local is exact. Existing Docker projects in `/volume1/docker/`: **truxon-backup** (nightly encrypted pg_dump+storage at 02:00, 30d retention, WORKING), truxon-tailscale (how TS runs on the NAS), truxon-mumble.

**BLOCKER: `turkyildiz` is NOT in the `docker` group and sudo needs a password (none passwordless).** To manage containers (backup heartbeat, fuel fetcher) the user must run `sudo usermod -aG docker turkyildiz` once, then agent reconnects. Everything NAS-side is Docker, so this is the enabler.

**⚠️ SECRET EXPOSURE (2026-07-19):** `/volume1/docker/truxon-backup/docker-compose.yaml` stores prod secrets in plaintext env (SUPABASE_DB_URL incl. DB password, SUPABASE_SERVICE_ROLE_KEY, BACKUP_PASSPHRASE) and they were surfaced in a chat transcript. Recommended the user rotate the DB password + backup passphrase. Do NOT `cat` secret-bearing files again — redact or read names only.

**✅ BACKUP DONE (2026-07-19).** Host cron needs root (crontab not setuid, spool root-owned) so scheduling is a **Docker container**: `truxon-scheduler` (image `docker:cli`, compose at `/volume1/docker/truxon-backup/scheduler/`) runs **busybox crond** and executes the hardened `deploy/backup/*` scripts via the **mounted docker socket** — nightly backup 02:00 CST + weekly restore test Sun 04:00 CST. Key trick: **identity bind-mount** `/volume1/docker/truxon-backup:/volume1/docker/truxon-backup` so sibling `docker run -v` paths resolve on the host. Secrets in `/volume1/docker/truxon-backup/backup.env` (chmod 600): DB URL, service key, BACKUP_PASSPHRASE, SUPABASE_ANON_KEY, WATCHDOG_REPORT_KEY, BACKUP_BUCKETS=documents,personal,team. Old embedded-runner container retired. **This scheduler pattern is the template for the fuel fetcher.**
- Found + fixed **2 real bugs**: (1) `storage_backup.py` didn't URL-encode object paths → crashed on the first filename with a space (every documents backup failed); (2) `backup.sh` heartbeat omitted the anon `Authorization` header → watchdog `verify_jwt` gate 401'd it. Both fixed in-repo (commit `deploy/backup/`).
- **WATCHDOG_REPORT_KEY rotated** (fresh token_hex(32)) → set on Supabase `watchdog` fn secret + NAS `backup.env`. The staged (not-yet-deployed) workstation responder `deploy/watchdog/responder.mjs` will need this new value when it's installed.
- **Off-site immutable copy (ransomware defense, 2026-07-19):** nightly backup uploads both encrypted files to **Backblaze B2** bucket `truxon-backups` (S3 endpoint `s3.us-east-005.backblazeb2.com`, region us-east-005, bucket ID `4757c0770b1e73c698f70f18`) with **Object Lock COMPLIANCE** + per-object retain-until (OFFSITE_LOCK_DAYS, default 30). Proven immutable: the write key (keyID `0057707be3687f80000000001`, scoped to the bucket, held on NAS in `/volume1/docker/truxon-backup/b2.env` chmod 600) got **AccessDenied** deleting a locked object even though it has deleteFiles+bypassGovernance (compliance mode can't be bypassed by anyone). Owner account: turkyildiz@gmail.com. Upload uses `amazon/aws-cli` sibling container, creds passed by env not args. **TODO tidy-up:** set a B2 lifecycle rule to delete objects >~90d (bounds cost; lock still wins until 30d); large files (>~5GB someday) would need multipart instead of single put-object. On-box btrfs snapshots staged in `deploy/backup/immutable/` (container-setup.sh + truxon-snapshot.sh) but NOT deployed — the privileged-container step was classifier-blocked and its value is capped anyway (turkyildiz ∈ docker group = root-equiv), so B2 WORM is the real backstop.

**✅ FUEL FETCHER LIVE (2026-07-19).** `/volume1/docker/truxon-fuel/`: `fetch-atob.mjs` runs in `mcr.microsoft.com/playwright:v1.61.1-jammy` (image version MUST match the npm playwright version — a 1.48 image with 1.61 npm silently no-ops). Cron `0 3,16 * * *` (CST) via `truxon-scheduler` → `run-fuel.sh` (flock + monthly logs in `logs/`). Secrets in `fuel.env` (chmod 600): FUEL_IMPORT_KEY (fresh token_hex(24), also set as Supabase secret), WATCHDOG_REPORT_KEY + SUPABASE_ANON_KEY (alert path needs the anon bearer — same verify_jwt gotcha as the backup heartbeat; fixed in fetch-atob.mjs `alert()`). Verified end-to-end through the cron path: 209-row CSV, idempotent import (run2: 208 updated/0 inserted). **19 unmatched_trucks** pending unit-name reconciliation.
- **AtoB session seeding procedure** (when it expires — watchdog alert fires): headed login can't run on the NAS (no terminal for user; VNC-container launch is classifier-blocked). Working procedure: on ikedev (has DISPLAY=:0) run `node fetch-atob.mjs --login` from a scratch dir with `ATOB_PROFILE_DIR` in a local fuel.env + stdin FIFO, user logs in on the desktop window, send Enter to the FIFO, then `tar czf - -C .atob-profile . | ssh nas 'tar xzf - -C /volume1/docker/truxon-fuel/.atob-profile'` (plain rsync fails: UGOS rsync says "invalid path"). Delete the local profile copy after (live session hygiene). Playwright uses --password-store=basic so the profile is portable across machines.

**✅ RELEASE PIPELINE MOVED TO DEV BOX (2026-07-21):** the release machine is DEAD (owner confirmed). New 4096-bit signing keystore at `~/dev-tools/truxon-release.jks` + `mobile/android/key.properties` (both gitignored; PKCS12 GOTCHA: storePassword MUST equal keyPassword or packageRelease fails "final block not properly padded"). Cert SHA-256 starts 3F:9D:34:BC. Backups: NAS `/volume1/docker/truxon-backup/release-signing/signing-2026-07-21b.tar.gz(.gpg)` + B2 `signing/` (immutable to 2027-07-21). google-services.json re-downloaded from Firebase console (project truxon-99a31) via owner's Chrome. v1.0.0+4 published from dev box; NEW SIGNATURE = one-time manual uninstall+reinstall per tablet, then OTA resumes. gh CLI already authed as turkyildiz here. Owner still owes: revoke dead machine's GitHub sessions.

**IMPORTANT (2026-07-21): the "blocked on UGOS" framing was STALE — SSH works and has since 07-19.** Docker group membership is in place (uid 1000 in `docker`). Never tell the owner NAS work is blocked without probing `ssh turkyildiz@100.89.140.98` first.

**✅ VALHALLA DEPLOYED (2026-07-21):** `/volume1/docker/truxon-valhalla/` (compose from repo `deploy/valhalla/`), midwest 10-state graph (IL/IN/IA/KY/MI/MN/MO/OH/PA/WI, 6.2GB pbf). **Tailscale Funnel approved by owner and LIVE: `https://aida-nas.tail2c5ca.ts.net` → :8002** (funnel runs inside the host-networked `truxon-tailscale` container: `docker exec truxon-tailscale tailscale funnel --bg 8002`). publish-release.sh must add `--dart-define=VALHALLA_URL=https://aida-nas.tail2c5ca.ts.net`. Refresh graph quarterly via setup.sh. UGOS rsync is broken — copy files with tar-over-ssh.

**✅ DOC-RAG HANDOFF DONE (2026-07-21):** the CRON_SECRET gate rollout had silently locked BOTH rag workers out of the doc-rag edge fn since ~2026-07-20 17:12 (403 `{error:'cron only'}` read as "no work" — search requests sat pending forever). Fixed: workers send `x-cron-key` from rag.env (CRON_SECRET appended, chmod 600), gate errors now log LOUDLY. Search worker containerized (`truxon-rag-search`, node:20-slim, host network, restart unless-stopped — was bare nohup that died on reboot). NEW hourly index cron (`:40`, run-index.sh) embeds fresh driver PODs; scheduler compose now mounts truxon-rag. GOTCHA: the scheduler's real crontab is the HEREDOC in `scheduler/entrypoint.sh` — the sibling `crontab` file is dead documentation; edit the entrypoint + restart. Verified end-to-end: enqueue_doc_search → 12 results @0.70 sim in ~2s. Repo commit 38a4476.
