---
name: security-posture
description: "Truxon security architecture after the 2026-07-20 P0 remediation — cron secret, inactive lockout, money-path locks; what's still open"
metadata:
  type: project
  originSessionId: a28d9126-d517-4423-90d2-26d2f9088c49
  modified: 2026-07-24T16:37:50.174Z
---

Two third-party security reports (Grok, in `docs/Truxon_Code_Security_Scrutiny_Report.docx` + `docs/Truxon_Full_Rescan_Security_Code_Report.docx` — the owner drops these into the repo) drove the 2026-07-20 P0 batch (migrations 20260720640001/660001, commits 3637a10+f1bf85c):

**⚠ OWNER LOST THE CRON_SECRET PLAINTEXT (2026-07-24)** — it's still LIVE (edge env + `app_private.cron_config` DB copy both intact, so nothing is broken), but the owner has no copy, so it can't be used to manually hit cron endpoints or rotated cleanly until reset. It's a bcrypt-style opaque value nobody can read back. **TODO: reset it (secrets set + watchdog setter, see §rotate below) and store it in the [[secrets-vault]] this time** — same root cause as the lost Jul-21 admin passwords (values lived only on the old dev box, wiped by the machine swap). The [[secrets-vault]] now holds the file secrets (OTA key, keystore, google-services) but NOT the text secrets yet — populate CRON_SECRET / service_role / DB password / account logins via the GUI.

**CRON_SECRET (S-01, was CRITICAL):** the public anon JWT is NO LONGER authorization for privileged edge doors. `requireCron(req)` in `_shared/auth.ts` checks the `x-cron-key` header against the `CRON_SECRET` edge env (fail closed) across 11 functions + db-backup. All edge cron jobs go through `app_private.cron_edge_call(fn, body)` which reads the DB-side copy from `app_private.cron_config`. **The secret is NOT in git**: edge side via `supabase secrets set CRON_SECRET=…`; DB side seeded through the watchdog's admin-JWT-gated `{set_cron_secret: …}` mode which calls the service-only `set_cron_config()` RPC. If the secret must rotate: generate, `supabase secrets set`, redeploy is NOT needed (env read per-invocation), then call the watchdog setter. **The NAS doc-rag worker will need this key when NAS access returns** (its calls are gated now).

**my_role() RAISES 'Account disabled'** for inactive/missing profiles (S-06) — every RLS policy and role gate fails closed; service/cron (auth.uid() null) still gets NULL. Do not "fix" gates to tolerate null-role for authenticated users — the raise is the mechanism. SPA cold-restore signs deactivated sessions out. DB trigger `profiles_protect_last_admin` blocks demoting/deactivating the last active admin.

**Money paths:** `void_invoice` reopens invoiced accessorials (void→re-bill keeps detention); `create_invoice` row-locks accessorials and invoices only the locked ids; detention proposals refresh amounts while still 'proposed'. Mobile `signOut()` stops the GPS foreground service and wipes gps_queue/status_outbox.

**Third report (`docs/Truxon_Ground_Truth_Security_Report.docx`, exact-count methodology, verified accurate) closed the deferred MEDIUMs on 2026-07-20 late-night (commits 86daac0 + bf121f5):** S-09/GT-04 — CORS now reflects Origin only for truxon.com/www/localhost:5173 (+`CORS_EXTRA_ORIGINS` edge env) via AsyncLocalStorage in `_shared/auth.ts`; every function wraps its handler in `withCors()` (new functions MUST too, or they fall back to truxon.com-only). S-08/GT-05 — QBO connect uses a one-time 2-min sha256-hashed ticket (POST `mode=connect_ticket`, admin), never a JWT in the URL. S-11/GT-06 — `profiles` SELECT narrowed to self + office roles (migration 720001; pgTAP 71). Also: ops secrets off argv (backup.sh PG* env split, ADMIN_EMAIL/ADMIN_PASSWORD env-only in mjs scripts), go-live env parsed without shell eval, OTA apkUrl https+GitHub-host allowlisted (manifest signing still the honest gap), `next_invoice_number` revoked from authenticated / `next_load_number` office-gated in-function (blanket revoke would break the invoker-rights loads trigger — pgTAP 70). GT-C1 (responder autofix) confirmed OFF. Vercel CSP/HSTS headers live (rainviewer + OSM/carto allowlisted).

**Gotchas learned:** local `db reset` does NOT reproduce prod's default table grants (authenticated lacked SELECT on profiles locally; prod has it) — RLS tests must `grant` inside their rolled-back transaction. `frontend/.env.local` anon key had gone stale post-rotation (probes said "Invalid API key"); refreshed from scratchpad exam creds — a 401 from a probe is only meaningful with a known-good key.

**2026-07-21 six-pass review (docs/Truxon_Code_Review_2026-07-21.docx, commit d36e78a fixes): 0 open CRITICAL/HIGH security holes; all prior fixes verified held.** FIXED same night: H-1 detention-stranded-on-billed-load revenue leak (propose filter + critical `stranded_accessorial` sentinel + pgTAP 90 — $0 was stranded, trap closed pre-first-dollar); H-2 responder AUTOFIX hard gate (needs RESPONDER_AUTOFIX_CONFIRM second env + allowlist WITHOUT git push, skip-permissions removed; verified unset everywhere); M-5 PrePass SFTP ed25519 host-key pin (PREPASS_HOSTKEY in NAS tolls.env, fail closed); M-6 four scripts creds argv→ADMIN_EMAIL/ADMIN_PASSWORD env; llm-proxy timingSafeEqual (live on NAS). **Review M/LOW backlog — CLEARED across the 2026-07-22 48h autonomous run (commits bf9498c/f16dcaa/0f15cf0/402e481/dc70e99):** M-1 invoice preview now folds approved accessorials + a POD-before-billing warning (dc70e99); M-2 sign-out GPS race fixed via cross-isolate kGpsSignout flag + isRunningService poll; M-3 refresh token → Android keystore (SecureLocalStorage/SecureSession, plaintext→keystore migration, both isolates); M-7 pinned Dockerfiles (truxon-tolls paramiko==3.4.0, truxon-vision poppler baked) + conditional installs. LOWs done: constant-time `timingSafeEqualStr` across fuel/toll/notify/watchdog; `listAllAuthUsers` pagination (trux-sentinel/inbox/admin-users); unregisterPushToken + diag_log/radio-name wipe on signout; protect_last_admin DELETE branch; trux_query gated off drivers; equipment search sanitizer; go-live.sh no-eval parser; `/tmp/*.lock`→app-owned `.run.lock`; handle_new_user defaults to `driver`; quote-request cooldown → DB `check_ip_rate_limit`; config.toml "anon bearer"→CRON_SECRET comments; qbo-sync dead `auth` vars removed. **M-4 OTA manifest signing — CLOSED 2026-07-24 (commit ca271d5):** publish-release.sh now signs the canonical manifest fields (versionCode/versionName/apkUrl/sha256/rolloutPct) with the owner's OFFLINE Ed25519 key (openssl `pkeyutl -rawin`, key from `TRUX_OTA_SIGNING_KEY`, never in repo); the app embeds the raw 32-byte public key (`AppConfig.otaSigningPublicKey`) and `verifyManifestSignature`/`canonicalManifestPayload` in update_service.dart reject any unsigned/forged/downgraded/redirected manifest before trusting it (empty key = disabled for backward-compat so the transition release still installs on current tablets). ed25519_edwards dep; 9 pgTAP-equiv Dart tests; mobile suite 108 green. Private key held in owner's KeePassXC; `*.pem` gitignored. Sequencing: first signed release installs on current (non-verifying) tablets, all releases after are signed-or-refused. **STILL OPEN:** the service-role emailed-attachment filing refactor (deferred as too big for a sweep). **A self-inflicted regression to watch:** block-2's trux_query redefinition dropped the honeypot decoy-refusal and I pushed it to prod before running the FULL pgTAP suite; caught + fixed via migration 006002. Lesson: run the whole pgTAP suite before pushing any SQL, not targeted smokes.

**MFA/TOTP DARK-LAUNCH LIVE 2026-07-22 (commit 3e88abd):** native Supabase MFA (`supabase.auth.mfa.*`), opt-in enroll/verify/manage card on the admin `/security` page, login NOT yet gated on it. data.ts: mfaListFactors/mfaEnrollTotp (clears stale unverified first)/mfaVerifyTotp/mfaUnenroll. Needs "Authenticator app (TOTP)" enabled in project Auth settings (default on). Widen to a per-user Account screen + flip AAL2 enforcement later.

**DML RANSOMWARE GUARD LIVE 2026-07-22 (commit 0f15cf0; migration 20260722006001; pgTAP 94):** extends the DDL guard to row-level destruction. `guard_bulk_delete` (AFTER DELETE, transition table) BLOCKS >100-row deletes on crown jewels (invoices/loads/customers/fuel/tolls/drivers/trucks — trux_insights excluded); `guard_bulk_update` ALARM-ONLY on >500-row updates (never blocks, to protect legit QBO syncs/backfills). Bypass `set app.allow_bulk_dml='on'`. OOB alarm via dblink for the blocked path. **Bulk maintenance on crown jewels now needs app.allow_bulk_dml='on' or it's blocked/flagged.**

**Security Console LIVE 2026-07-22 (commit c1dd52f):** admin `/security` page — one `security_console()` RPC surfaces guard-armed, audit-chain-intact, lockdown, honeytoken count, canary liveness, drift baseline, open criticals, audit tail; break-glass lockdown toggle + re-bless baseline buttons.

**Account passwords rotated to strong 2026-07-21** (were weak test values testpass1/password1/password2): ike (admin), dave + elliot (dispatchers), driver1 + driver2 (driver tablets). Owner `turkyildiz@gmail.com` untouched. Reset via GoTrue admin API `PUT /auth/v1/admin/users/{id}` using the service key from NAS `/volume1/docker/truxon-backup/backup.env`. Do NOT re-flag these as weak. Current values handed to owner in chat + session scratchpad only, never git/memory.

**HONEYPOTS LIVE 2026-07-22 (owner's idea; migrations 20260722001001/2, commit 9164473, pgTAP 91):** decoy `public.api_keys` + `public.bank_accounts` views over SECURITY DEFINER `_hp_*` fns serving fake data; NOTHING legitimate reads them — **never SELECT them (psql/Studio/gen-anything) or you page the owner**; `trux_query` refuses them by name. Trips record who (claims/session_user/IP/UA) to `app_private.honeypot_hits` and kick an instant sentinel scan (throttled 10min). Capture is rollback/read-only-proof: primary path is autonomous **dblink** write using DSN in `cron_config` key `hp_dsn` (set via set_cron_config at deploy, not in git). Severity: anon=warn, authenticated/service_role/direct-DB=critical (pushes). Sentinel keeps object/day findings 30d; insight_detail shows per-hit evidence. Gotchas: view fns execute with CALLER privileges (grants needed — test AS `authenticated`, superuser masks it); GitHub push protection flags realistic fake keys (concatenate literals); local dblink fails under trust auth (prod scram ok, verified live via=dblink). A warn finding from my 2026-07-22 live-fire drill was left open as a demo.

**SECURITY-OPS BATCH LIVE 2026-07-22 (commit 0062204; migrations 20260722002001-4; pgTAP 92, 14 asserts; all on prod + trux-sentinel redeployed):**
- **Tamper-evident audit log** `app_private.security_audit` — append-only (immutability trigger + revoke from all incl service_role), sha256 **hash chain** (row_hash = digest(prev||at||event||sev||uid||detail)). `public.security_audit_verify()` walks it → `{intact,broken_at_id}` (admin/service gated); `security_audit_recent(n)` office-gated for UI. `app_private.audit(event,sev,detail)` is the ONLY writer (definer; advisory-lock serialized). Records role_change/admin_granted/invoice_void/lockdown/honeytoken/baseline events.
- **Role-escalation tripwire** — trigger `profiles_role_tripwire` audits any →admin as critical + fires an immediate scan; the FINDING is produced by sentinel_scan reading the audit log (kept out of the trigger to avoid polluting insight counts in non-scan test paths — that was the fix when it broke test 18).
- **Salting/honeytokens** `app_private.honeytokens` (sha256 of the 4 decoy keys from `_hp_api_keys`, **plaintext never in git** — stored as hardcoded hex). `public.honeytoken_seen(hash)` (authenticated+service, NOT anon → stays off the drift baseline) audits+finds+pushes on match. Wired: shared `requireCron` failure → `maybeHoneytoken(got)` fire-and-forget hashes the rejected secret, POSTs `{mode:'honeytoken',hash}` to trux-sentinel (authenticated with our OWN CRON_SECRET) → new sentinel honeytoken mode calls the RPC.
- **Canary account** `ap-archive@aidalogistics.com` uuid `...ca11`, permanently `is_active=false` (handle_new_user auto-creates active → migration force-deactivates). Any GoTrue auth activity for it = user-list enumeration; sentinel scans `auth.audit_log_entries` for the email. DON'T sign in as it.
- **Posture-drift sentinel** — `app_private.security_baseline` snapshots anon/authenticated table grants + anon routine grants + RLS-off public tables via `security_posture()`; scan fires on anything in live-EXCEPT-baseline (anon/routine=critical, authenticated/rls_off=warn). `bless_security_baseline()` admin-only + audited. Automates the report bug-class.
- **Break-glass** `app_private.system_flags` + `set_lockdown(bool,reason)` (admin, audited critical) freezes profiles role/is_active/insert changes (guard trigger; service_role exempt to recover). `public.system_status()` for UI banner. FULL read-only freeze = `alter role authenticator set default_transaction_read_only=on` (superuser) — see `deploy/SECURITY_RUNBOOK.md`.
- **CI gitleaks** (.github/workflows/ci.yml `secrets` job + .gitleaks.toml) catches a REAL key pre-push; decoys concatenated/hashed + path-allowlisted; anon JWT regex-allowlisted.
- Gotcha: pgcrypto `crypt`/`gen_salt` must be `extensions.`-qualified in migration DDL (DO blocks/seeds) — `supabase db push` runs with a restricted search_path and fails where psql (postgres search_path) succeeds. `digest` too.
- I ran live-fire smoke tests on prod (honeytoken match → fired a critical + push, then resolved the finding; the immutable audit row stays). The canary/honeypot/lockdown all verified.

**JADEPUFFER/ENCFORGE RANSOMWARE DEFENSE LIVE 2026-07-22 (commit a2aac48; migrations 20260722003001/2; pgTAP 93; docs/THREAT_JADEPUFFER.md).** Agentic ransomware (Sysdig, 2026-07) whose finale is prod-DB destruction. Recon: **no Langflow/Nacos/MinIO** on NAS, Ollama bound 127.0.0.1 — actual CVE vectors absent.
- **Ransomware guard**: `sql_drop` event trigger `guard_destructive_ddl_trg` BLOCKS DROP TABLE/SCHEMA on public non-temp objects + `guard_truncate` BEFORE-TRUNCATE triggers on crown jewels (invoices/loads/customers/fuel_transactions/toll_transactions/trux_insights/drivers/trucks). Rolls back + alarms **out-of-band via dblink** (`ransom_guard_record` → audit critical + finding + push, survives the rollback). Bypass for legit migrations: `set app.allow_drops='on'`. Service-key/authenticator CANNOT drop the event trigger (ownership=postgres); only a postgres/DB-URL compromise could disable it. Verified on prod (real DROP blocked). **Future migrations that drop a public table MUST `set app.allow_drops='on'` first** or they'll be blocked.
- **Canary env token**: edge secret `LEGACY_WORKER_KEY`=twxlegacy_... (only sha256 committed) registered as honeytoken → counters env secret-sweep; replay trips requireCron honeytoken alarm.
- **Backups = ransomware-resilient to cloud compromise**: 3 copies — Supabase / NAS-local (truxon-backup container PULLS nightly pg_dump+storage, GPG-AES256, 30d) / Backblaze B2 offsite. NAS pull-based → cloud/service-key attacker can't reach in to delete.
- **RESIDUAL RISK = NAS compromise**: NAS `docker-compose.yaml` holds service key + BACKUP_PASSPHRASE + B2 keys INLINE (plaintext env) next to internet-facing media apps (Plex/Sonarr/Radarr/Sabnzbd/Jellyfin/Mumble). **OWNER ACTION ITEMS** (in threat doc): (1) enable **B2 Object Lock** (immutable retention — THE anti-ransomware backup control), (2) move NAS secrets to root-600 `env_file` not inline compose, (3) isolate/firewall NAS media stack from backup vault, (4) `sudo sh deploy/security/ioc-block.sh` (blocks C2 45.131.66.106 / exfil 64.20.53.230; no passwordless sudo on NAS), (5) MFA. IOC block script + ransomware runbook section shipped.

**POSITIVE-FORM GATE SWEEP LIVE 2026-07-23 (commit 45554e1; migrations 20260723001001/2; pgTAP 149; suite 883/883; pushed to prod):** all 22 functions whose latest definition used the fragile `if auth.role() <> 'service_role' and my_role() not in (...)` idiom (NULL auth.role() → NULL condition → silent pass) redefined from canonical `pg_get_functiondef` to the positive form `if not (coalesce(auth.role(),'') = 'service_role' or my_role() in (...))` per the dwell_league template. sentinel_scan kept its 120s statement_timeout pin. **New functions must use the positive form.** Residual nuance (accepted, matches template): when `my_role()` itself is NULL (auth.uid() null — bare superuser SQL only), `NULL in (...)` still makes the gate NULL-pass; not API-reachable (anon lacks EXECUTE — verified; authenticated always has sub, and no-profile raises 'Account disabled'). The sweep exposed a latent prod bug: driver-facing `my_week_scorecard` called office-gated `detention_events` (drivers would be denied in prod where auth.role()='authenticated'); fixed by splitting ungated `detention_events_core` (EXECUTE revoked from anon/authenticated, service_role + definer-only) behind the gated wrapper. Test 101 had leaned on the loophole (accountant calling admin-gated sentinel_scan) — now admin.

**CRON_SECRET rotation drill (#3) — pre-check PASSED, live rotation DEFERRED 2026-07-24:** live-probed all **17** requireCron doors with a bogus x-cron-key → every one REJECTED (13× `401`, report-send/dispatch-watch `403 'cron only'`, doc-rag/qbo-sync `400` from empty-body action-routing before the gate). Gate armed fleet-wide; the 401-vs-403-vs-400 variance is cosmetic (all reject, nothing gets through) — a consistency cleanup candidate, not a hole. Rotation procedure is documented + correct (`deploy/SECURITY_RUNBOOK.md §4`: `openssl rand -hex 32` → `supabase secrets set CRON_SECRET` → watchdog `{set_cron_secret}` admin-JWT setter → verify a door accepts the new key). Owner chose NOT to rotate the live secret pre-launch (no compromise, don't change a working secret without cause) — capability proven, execution on-demand. Probe used a SHORT bogus key on purpose (requireCron's `maybeHoneytoken` only fires on rejects ≥20 chars, so the probe stayed quiet).

Off-site backup: `db-backup` edge fn dumps 26 tables gzipped to the private `db-backups` bucket nightly 03:37, 30-day retention (independent of the NAS pipeline).

Related: [[project-truxon]], [[trux-dispatch-shadow]], [[northstar-project]].
